export interface Destination {
  id: string;
  name: string;
  slug: string;
  region: string;
  altitude: number;
  best_season: string;
  description: string;
  image_url: string;
  package_count: number;
}
